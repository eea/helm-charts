apiVersion: v2
name: matrix-webhook
description: Receives HTTP webhooks and forwards them as messages to Matrix rooms, using a standard Matrix user account (no homeserver admin rights required).
type: application
version: 0.1.1
appVersion: "latest"
keywords:
  - matrix
  - webhook
  - notification
