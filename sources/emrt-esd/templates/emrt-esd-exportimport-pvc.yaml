apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emrt-esd-exportimport
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: emrt-esd-exportimport

spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi

