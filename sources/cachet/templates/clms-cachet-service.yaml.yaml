# clms-cachet-service.yaml
{{- if .Values.clmsCachet.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: clms-cachet
spec:
  selector:
    app: clms-cachet
  ports:
    - name: http
      port: 80
      targetPort: 8000
{{- end }}
