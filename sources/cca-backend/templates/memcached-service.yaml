{{- if .Values.links.createPostgresService }}
apiVersion: v1
kind: Service
metadata:
  name: "memcached"
spec:
  type: ExternalName
  ports:
    - name: memcache
      port: 11211
  externalName: "{{ .Values.links.memcached }}.svc.cluster.local"
{{- end }}
