{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cca-decode-folder-filter
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: cca-decode-folder-filter
  annotations:
    nginx.ingress.kubernetes.io/configuration-snippet: |
      deny all;
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
  {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.plone.hostname }}
      http:
        paths:
          - path: /decodeFolderFilter
            pathType: Exact
            backend:
              service:
                name: {{ .Values.varnish.serviceName }}
                port:
                  number: {{ .Values.varnish.port }}
  {{- if .Values.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.plone.hostname }}
      {{- if .Values.ingress.certificate }}
      secretName: {{ .Values.ingress.certificate }}
      {{- end }}
  {{- end }}
{{- end }}
