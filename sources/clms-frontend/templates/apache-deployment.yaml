{{- if and .Values.apache .Values.apache.enabled }}
{{- if .Values.apache.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-apache-config
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: apache
data:
  vh-my-app.conf: |
{{ .Values.apache.config | replace "\t" "    " | indent 4 }}
---
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-dpl-apache
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: apache

spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "appl.selectorLabels" . | nindent 6 }}
      component: apache
  template:
    metadata:
      labels:
        {{- include "appl.selectorLabels" . | nindent 8 }}
        component: apache
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "appl.serviceAccountName" . }}
      containers:
      - name: apache
        image: "{{ .Values.apache.image.repository }}:{{ .Values.apache.image.tag }}"
        env:
        - name: TZ
          value: "{{ .Values.timezone }}"
        - name: APACHE_MODULES
          value: "{{ .Values.apache.modules }}"
        {{- if .Values.apache.config }}
        - name: APACHE_CONFIG_URL
          value: "file:///mnt/apache-config/vh-my-app.conf"
        {{- end }}

        ports:
        - name: http
          containerPort: 80

        {{- if .Values.apache.livenessProbe }}
        livenessProbe:
          {{- toYaml .Values.apache.livenessProbe | nindent 10 }}
        {{- end }}

        {{- if .Values.apache.startupProbe }}
        startupProbe:
          {{- toYaml .Values.apache.startupProbe | nindent 10 }}
        {{- end }}

        resources:
          {{- toYaml .Values.apache.resources | nindent 10 }}

        {{- if or .Values.apache.config .Values.apache.staticContent.enabled }}
        volumeMounts:
        {{- if .Values.apache.config }}
        - name: apache-config
          mountPath: /mnt/apache-config
          readOnly: true
        {{- end }}
        {{- if .Values.apache.staticContent.enabled }}
        - name: static-content
          mountPath: /staticcontent
        {{- end }}
        {{- end }}

      {{- if or .Values.apache.config .Values.apache.staticContent.enabled }}
      volumes:
      {{- if .Values.apache.config }}
      - name: apache-config
        configMap:
          name: {{ .Release.Name }}-apache-config
      {{- end }}
      {{- if .Values.apache.staticContent.enabled }}
      - name: static-content
        persistentVolumeClaim:
          claimName: {{ .Values.apache.staticContent.existingClaim | default (printf "%s-apache-static" .Release.Name) }}
      {{- end }}
      {{- end }}

      {{- with .Values.apache.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.apache.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.apache.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Always
{{- end }}
