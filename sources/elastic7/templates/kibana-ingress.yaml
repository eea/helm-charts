{{- if and .Values.kibana.ingress.enabled .Values.kibana.enabled }}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "appl.fullname" . }}-kibana-ingress
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: kibana-ingress
  annotations:
  {{- if .Values.kibana.ingress.letsencrypt }}
    cert-manager.io/cluster-issuer: "{{ .Values.kibana.ingress.letsencrypt }}"
  {{- end }}
  {{- range $key, $value := .Values.kibana.ingress.configuration }}
    {{- if ($value) }}
    nginx.ingress.kubernetes.io/{{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
  {{- with .Values.kibana.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}

  {{- if .Values.kibana.ingress.restrictFromInternet }}
    nginx.ingress.kubernetes.io/whitelist-source-range: |
      192.168.60.0/22,
      192.168.64.0/21,
      10.92.24.0/21,
      10.50.0.0/16,
      10.20.30.0/24,
      10.0.9.0/24,
      10.50.7.57
  {{- end }}

spec:
  ingressClassName: {{ .Values.kibana.ingress.className }}
  rules:
  - host: {{ .Values.kibana.url }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name:  {{ .Release.Name }}-kibana
            port:
              number: 5601
  {{- if .Values.kibana.ingress.tls }}
  tls:
  - hosts:
    - {{ .Values.kibana.url }}
    {{- if .Values.kibana.ingress.certificate }}
    secretName: {{ .Values.kibana.ingress.certificate }}
    {{- end  }}
  {{- end  }}
{{- end }}
