{{- if and .Values.esworker.ingress.enabled .Values.esworker.enabled }}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "appl.fullname" . }}-esworker-ingress
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: esworker-ingress
  annotations:
  {{- if .Values.esworker.ingress.letsencrypt }}
    cert-manager.io/cluster-issuer: "{{ .Values.esworker.ingress.letsencrypt }}"
  {{- end }}
  {{- range $key, $value := .Values.esworker.ingress.configuration }}
    {{- if ($value) }}
    nginx.ingress.kubernetes.io/{{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
  {{- with .Values.esworker.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}

  {{- if .Values.esworker.ingress.restrictFromInternet }}
    nginx.ingress.kubernetes.io/whitelist-source-range: |
      192.168.60.0/22,
      192.168.64.0/21,
      10.92.24.0/21,
      10.50.0.0/16,
      10.20.30.0/24,
      10.0.9.0/24,
      10.50.7.57
  {{- end }}

spec:
  ingressClassName: {{ .Values.esworker.ingress.className }}
  rules:
  - host: {{ .Values.esworker.url }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name:  {{ .Release.Name }}-esworker
            port:
              number: 5601
  {{- if .Values.esworker.ingress.tls }}
  tls:
  - hosts:
    - {{ .Values.esworker.url }}
    {{- if .Values.esworker.ingress.certificate }}
    secretName: {{ .Values.esworker.ingress.certificate }}
    {{- end  }}
  {{- end  }}
{{- end }}
