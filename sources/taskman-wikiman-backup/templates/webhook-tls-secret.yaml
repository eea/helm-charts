apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-webhook-tls
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.webhook.tls.cert }}
  tls.key: {{ .Values.webhook.tls.key }}