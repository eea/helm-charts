apiVersion: v1
kind: Service
metadata:
  name: "postgres"
spec:
  type: ExternalName
  {{- if .Values.links.external }}
  externalName: "{{ .Values.links.database }}"
  {{- else }}
  externalName: "{{ .Values.links.database }}.svc.cluster.local"
  {{- end }}


