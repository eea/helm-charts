---
apiVersion: v1
kind: Service
metadata:
  name: redmine
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: redmine
spec:
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  type: ClusterIP
  selector:
    {{- include "appl.selectorLabels" . | nindent 4 }}
    component: redmine

