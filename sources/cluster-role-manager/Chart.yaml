apiVersion: v2
name: cluster-role-manager
description: "App that creates the clusterRole and a clusterRoleBinding to an existing service account"
icon: https://raw.githubusercontent.com/eea/helm-charts/main/sources/cluster-role-manager/kubernetes.png

type: application

keywords:
- Role
- ServiceAccount
- ClusterRole
- ClusterRoleBinding

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.0.1

