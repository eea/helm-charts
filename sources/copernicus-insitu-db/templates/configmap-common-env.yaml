apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "appl.commonEnvConfigMapName" . }}
  labels:
    {{- include "appl.labels" . | nindent 4 }}
data:
  {{- range $key := keys .Values.commonEnv | sortAlpha }}
  {{ $key }}: {{ index $.Values.commonEnv $key | quote }}
  {{- end }}
